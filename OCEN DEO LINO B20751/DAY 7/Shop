#Eunice supermarket
#My shopping cart
#Letting the user input his own amount also in the price listing

Egg = float(input("Enter the cost of the egg: $"))
DoubleAABatteries = float(input("Enter the cost of the DoubleAAbatteries: $"))
Chicken = float(input("Enter the cost of the chicken: $"))
magazines = float(input("Enter the cost of magazine: $"))

# allowing the user to input his/her total cost
totalcost = float(input("Enter the total cost of the shopping cart: $"))

# checking to see if the total cost is eligable for a discount
if totalcost >= 50:
    discount = 0.10 # which is 10 percent
    discountcost = totalcost - (totalcost*discount)
    print(f"10% discount applied. Discounted total cost : ${discountcost:.2f}")
else:
    print(f"No discount applied. Total cost: ${totalcost:.2f}")

